version: '3.8'

services:
  intervals-bot:
    build: .
    container_name: intervals-icu-bot
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
